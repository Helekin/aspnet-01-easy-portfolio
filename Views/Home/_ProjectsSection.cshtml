@model IEnumerable<ProjectViewModel>

@if (Model is not null)
{
    <div class="text-center" style="padding-top:6rem;">
    <h1>Mis Proyectos</h1>
    <p>Aqui puedes apreciar mis recientes proyectos</p>
</div>

    <partial name="_ProjectsList" model="Model" />
}